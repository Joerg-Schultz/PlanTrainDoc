package de.tierwohlteam.android.plantraindoc_v1.repositories


/**
 *     How to access the repository functions
 *   val context: Context = ApplicationProvider.getApplicationContext()
 *   val repository = PTDRepository(context)
 *   val user = User(name = "Test User", email = "testuser@mail.de", password = "123", role = "standard")
 *   repository.insertUser(user)
 *
 *   TODO No Test yet,as this would generate a real Room database on device
 */